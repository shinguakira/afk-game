# 仕様書: *Idle Engineer*（仮）〜 残業の勇者 〜

IT企業を舞台にした放置型RPG。`Melvor Idle` のコアループ（1つの作業を選ぶ→自動で回る→XP/資源が貯まる→オフラインでも進む）を土台に、**題材をIT業務に丸ごと置き換える**。木こり・採掘ではなく「コードを書く・設計する・案件を倒す」。

> 既存エンジン（`client/src/game/`）はデータ駆動。スキル/アイテム/アクション/モンスターは `data/` の設定で定義する。本仕様はその構造に乗る形で書く。**v1 = 今のエンジンのリスキン + α**、それ以降を [将来拡張](#9-将来拡張ロードマップ) に分ける。

---

# ⭐ ゲームデザイン: 目的と流れ（現行の北極星）

> このセクションが「いま目指している形」。以降の §1〜 は個別メカニクスの詳細・経緯。
> 部下/チームは設計上は最終的に必要だが現状ノイズなので一旦実装削除済み（[起業メタ階層](#d-起業メタ階層終わらない終盤) で再登場予定）。

## 0-1. コアファンタジーと2本の軸
**「無名のコーダーが、技術を極め・金を稼ぎ、起業して伝説になる」**
プレイの動機は **2本の柱** が車の両輪:

| 軸 | 何を伸ばす | 代表的な目標 |
|---|---|---|
| **A. 技術探求** | 言語/フレームワーク/概念/資格を極める | ポリグロット、フルスタック、資格コンプ、全Lv99 |
| **B. 金稼ぎ** | 円・資産・収入を増やす | 初収入 → 月収UP → 年収1000万 → 億り人 → 資産10億 |

技術と金は相互に効く好循環:
`技術↑ → 良いプロダクト → 高い収益 → 円 → 設備/装備 → 効率↑ → 技術がさらに速く`。
起業（プレステージ）で両方をリセットし、永続倍率を得て次の周回へ。

## 0-2. 目的の3層（idleの定石）
| 層 | スパン | 目的 |
|---|---|---|
| 短期 | 数十秒〜 | 次のLv/解禁（FW・概念・レシピ・案件・PCティア） |
| 中期 | 1セッション | 昇進・装備更新・スタック習得・**初の継続収入** |
| 長期 | 横断 | 起業メタ進行・資産マイルストーン・称号・コンプ(100%) |

## 0-3. 全体ループ
```
[生産] 言語を書く → コミット
   │   └ 傘下を深掘り: 概念 / ライブラリ / フレームワーク / OSS / 資格   ……(軸A:技術)
   ▼
[制作] FW実装・料理・PC組み立て → プロダクト / 飯 / PC
   ▼
[経済] 売却 + 案件報酬 + サービス化(継続収入) → 円 ──────────┐    ……(軸B:金)
   │      └ 円の使い道: 食事・装備・PCパーツ・食材・(将来)事業    │
   ▼                                                              │ (設備で全部速く)
[遂行] 案件(戦闘) → 円・報酬・現場力XP                            │
   ▼                                                              │
[出世] 総合熟練度 → 昇進 → 職種/難案件/新システム解禁 ◀──────────┘
   ▼
[メタ] 起業(プレステージ) → ストック → 永続強化 → 上位段階で新システム解禁
```

## 0-4. 「方向」を生む4つの仕組み

### A. キャリア・ロードマップ（メインクエストの背骨）★最初に実装
順序付きマイルストーンを**常時1つ提示**。達成で報酬＋次を指す。
**技術(A)と金(B)のマイルストーンを交互に**織り込む:

1. 初コミット〔技術〕
2. 初売却で初収入〔金〕（まずはフリーランスで単発案件）
3. 初フレームワーク実装〔技術〕
4. 初案件クリア＋初報酬〔金〕
5. **会社に所属 → 月給＋会社ダンジョン解禁**〔金×案件〕★
6. ジュニア昇進
7. **初サービスリリース → 継続収入が発生**〔金〕★
8. 自作PC（エントリー）〔技術×金〕
9. 職種を選ぶ（ミドル）
10. 1スタック制覇（例: Web系を一定Lv）〔技術〕
11. 最初の会社を制覇（ダンジョン全クリア）〔案件〕
12. シニア昇進＋資格1つ〔技術〕
13. 資産1000万 / 月収◯◯〔金〕
14. テックリード
15. **3大目標へ突入**（[0-9](#0-9-最初の大目標3つのいずれかを達成)）: 🌙月を買う / 🏢でかい会社制覇 / 🧪新言語開発 → いずれか達成で章クリア→起業

> データ: `roadmap.ts`（id / 条件(達成判定) / 報酬 / 次の指示文）。UIは常時「次の目標」を1枚表示＋達成でトースト。
> 3大目標は終盤の分岐ゴール。それ以前は技術↔金↔案件を交互に踏ませて土台を作る。

### B. 解禁の可視化
今の暗黙Lvゲートを「あと3Lvで React 解禁」のように見せ、常に“もうすぐ届く何か”がある状態に。

### C. 称号・実績（=100%以外の収集軸）
ポリグロット(10言語Lv50) / フルスタック / OSSコントリビューター / 資格コレクター / 自作erマスター / 億り人 … 小バフ＋フレーバー。

### D. 起業メタ階層（終わらない終盤）
起業を繰り返すごとに大節目：**個人開発者 → スタートアップ → 上場 → ユニコーン**。
各階層で**新システムを解禁** ←ここで将来 **部下/チーム**（2〜3周目）・並行案件・事業経営 を戻す。

## 0-5. 金稼ぎ軸（軸B）の具体
- **円の稼ぎ方**
  1. 売却: コミット/プロダクト/不要装備（基礎収入）
  2. 案件報酬（メイン能動収入）
  3. **サービス化＝継続収入(MRR)**: 作ったプロダクトを「リリース」して放置中も円が増える。品質/種類で収益が変わる ← 金軸の肝
  4. （将来）事業・投資: 起業後の大型収入
- **資産マイルストーン**（金版ロードマップ）: 初任給 → 月収UP → 年収1000万 → 貯金1億 → 資産10億 …
- **円のシンク**: 食事・PCパーツ・装備・食材・（将来）オフィス設備・事業投資
- 技術↔金の結合: 技術が高いほど良いプロダクト→高MRR→設備→技術が速く（好循環）

## 0-6. 「100%」の位置づけ
全スキルLv99／全資格／全案件／図鑑コンプ = やり込みの終着点の**1つ**。
③称号 と ④起業メタ と ⑤資産 があるので、**100%が唯一のゴールではない**。

## 0-7. 実装順（軽い→重い・効果大→）
1. **A. キャリア・ロードマップ**（最優先・背骨）
2. **B. 解禁の可視化**（軽い・即効）
3. **サービス化＝継続収入**（金軸の肝）＋資産マイルストーン
4. **C. 称号・実績**
5. **D. 起業メタ階層**（大物・ここで部下/チーム復活）

## 0-8. 世界モデル: 案件=モンスター / 会社=ダンジョン
- **案件 = モンスター**。倒す＝納品。
- **会社 = ダンジョン**。会社は**案件グループ（順序付き＋最後に役員/上司ボス）**を保有する。全クリア＝**その会社を制覇**。
- **所属の自由**:
  - **フリーランス / アルバイト（無所属）**: いつでも**単発案件**を受けられる（常時供給・報酬控えめ・自由）。
  - **会社所属**: その会社の**固有案件（ダンジョン）**にアクセスでき、**月給（継続収入＝金軸に直結）**が入る。転職可。
- データ概形（実装時）:
  ```ts
  Company { id, name, tier(中小→メガベンチャー→大企業→外資/GAFA), projects: 案件[](末尾にボス), joinReq, salary(月給) }
  Freelance: 単発案件プール（プレイヤーLvでスケール・低報酬・所属不要）
  state.company: companyId | null   // null = フリーランス
  ```
- 既存の `案件(monsters)` はこの「単発プール」＋「各社ダンジョンの中身」として再編する。

## 0-9. 最初の大目標（3つのいずれかを達成）
3軸それぞれに“北”を置く。**どれか1つ達成で最初の章クリア**（＝起業/次周回のトリガ）。プレイスタイルで選べる:

| 軸 | 大目標 | 概要 |
|---|---|---|
| **金稼ぎ** | 🌙 **月（Moon）を購入** | 資産マイルストーンの最終到達点。とんでもない円のシンク＝富の証明。 |
| **案件** | 🏢 **でかい会社を制覇** | 最上位ティア企業のダンジョン（全案件＋ラスボス役員）をクリア。 |
| **技術** | 🧪 **新しい言語を開発** | 究極の技術プロジェクト。多数の言語を高Lvまで極め、特大の「言語開発」案件を完遂。 |

> 3つは並立。1つ達成すれば良いが、コンプ勢は全部狙える。達成内容で称号/周回ボーナスが変わる。

---

## 1. コンセプト / 世界観

新卒の見習いエンジニアが、**見習い → ジュニア → シニア → テックリード → CTO** と成り上がる出世idle。
ダンジョンは「案件（プロジェクト）」、雑魚は「バグ」「仕様変更」、ボスは「上司」。
HPは肉体ではなく **メンタル（精神力）**。0になると死ぬのではなく **燃え尽き（バーンアウト）** して強制離脱。回復は飯ではなく **カフェイン・有給・スルー力**。

風刺コメディのトーンで、用語のあるあるネタを全面に出す（例: 「仕様が確定しました（確定していない）」）。

---

## 2. コアループ

```
学習/コーディングで【知識・コード】を生産
        ↓
設計/実装で【機能・成果物】に組み立て
        ↓
案件（プロジェクト）に出撃して納品＝戦闘 → 【円(給料)・経験・評価】
        ↓
評価で 昇進 & クラスチェンジ → 補正で全体が強くなる
        ↓ (放置中も自動で回る)
```

Melvorの「1度に1作業しかできず暇」を緩和するため、**部下（[§7](#7-部下システム)）** に別作業を並行で回させられる。

---

## 3. 役職ランク と クラスチェンジ補正 ★本作の肝

### 3.1 役職ランク（縦の出世軸）
総合レベル（または「評価ポイント」）で昇進。ランクが上がると全ステ倍率が上がり、上位の案件・職種が解禁。

| ランク | 名称 | 解禁 |
|---|---|---|
| 1 | 見習い / インターン | 初期 |
| 2 | ジュニア | 案件「社内ツール改修」 |
| 3 | ミドル | 職種クラスチェンジ解禁 |
| 4 | シニア | 部下の採用解禁 |
| 5 | テックリード | 上位職、ボス「部長」 |
| 6 | マネージャ / アーキテクト | 並行案件 |
| 7 | CTO（エンディング/プレステージ枠） | ラスボス「経営会議」 |

### 3.2 職種クラス（横の特化軸）= 補正
ミドル昇進時に **職種** を選択。職種ごとに **スキル/戦闘ステに乗算補正** がかかり、ビルドの個性になる。**転職（クラスチェンジ）可能**だが、再修行コストあり。

```ts
// data/classes.ts
interface JobClass {
  id: string; name: string; tier: number;
  modifiers: Modifier[];          // 乗算/加算補正
  passive?: string;               // 特殊効果
  upgradesFrom?: string;          // 上位職への分岐
}
```

| 職種 | 補正（例） | パッシブ |
|---|---|---|
| フロントエンド | UI実装 +30% / インフラ -10% | 見た目の評価+（顧客ウケ↑） |
| バックエンド | ロジック実装 +30% / デザイン -15% | 大型案件で被ダメ減 |
| インフラ/SRE | 環境構築 +40% / 実装速度 -10% | 障害（DoT）耐性 |
| データサイエンティスト | 分析 +35% | レアドロップ率↑ |
| QA | テスト +40% | 敵の「バグ」出現を抑制 |
| PM | 全スキル +10% / 個別ピーク低い | 部下の効率 +25% |
| **フルスタック**(上位) | 全実装 +15%（穴がない） | 並行案件スロット+1 |
| **セキュリティ**(上位) | 防御系 +50% | 「炎上」を確率で無効化 |

> 補正は既存の `modifiers` 機構（プログレス§ の倍率）で表現可能。クラス変更時に再計算するだけ。

---

## 4. スキル = 実際の tech stack（分野 → 言語 → フレームワーク）★改訂

スキルは「設計/開発」のような粗い括りではなく、**実在の技術スタック**を個別にレベリングする。
**分野(domain)** でグルーピングし、各分野に **言語(language)** と **フレームワーク(framework)** がある。
`SkillKind` は内部的に gather=言語 / craft=フレームワーク / combat=現場力 に対応。

### 4.1 グルーピング（Melvor風・折りたたみ）
サイドバーは **言語 / フレームワーク / 現場力 / その他** のセクションに分かれ、
各グループは折りたたみ可能（`data/groups.ts`、`Sidebar` の `open` state）。

**言語(30) — 系統別グループ:**
| グループ | 言語 |
|---|---|
| スクリプト系 | JavaScript, TypeScript, Python, Ruby, PHP, Lua, Perl |
| システム系 | C, C++, Rust, Go, Zig |
| JVM/.NET系 | Java, Kotlin, Scala, C# |
| ネイティブ/モバイル | Swift, Dart, Objective-C |
| 関数型 | Haskell, Elixir, Clojure, F#, Erlang |
| データ/科学 | R, Julia, SQL |
| レガシー | COBOL, Fortran, Assembly |

**フレームワークは独立スキルではなく「言語スキル配下のアクション」**（`data/techtree.ts`）。
1つの言語スキルが Melvor 的に多数のアクションを持ち、レベルで段階的に解禁される:

| カテゴリ | 例（JavaScript） | 産出 |
|---|---|---|
| 基礎 | JavaScript を書く (Lv1) | コミット |
| 概念 | クロージャ / 非同期・Promise / イベントループ | コミット |
| ライブラリ | jQuery / Lodash / Axios | コミット×2 |
| フレームワーク | Node.js / Express / Vue / Svelte | コミット消費→プロダクト |
| OSS | OSS にコミット (高Lv) | 感謝 |
| 資格 | JavaScript 認定 (JSNAD) (Lv35) | 資格 |

> 全30言語が同様の配下ツリーを持つ（`techtree.ts` の `LANG_SPECS` に concepts/libraries/frameworks/oss/cert を書くと自動生成）。**解禁前のアクションも一覧表示し、ボタンは「Lv○○ で解禁（開始不可）」で無効化**。

### 4.2 ループ
- **言語を書く** → コミット産出、言語Lv↑。
- 概念/ライブラリ → さらにコミット、言語Lv↑（高効率だが高Lv解禁）。
- **フレームワークで実装** → コミット消費して **プロダクト**（高価）、言語Lv↑。
- OSS・資格 = 高Lvの到達目標（感謝・資格アイテム）。
- フレームワーク系アクションだけ「制作(craft)」補正、それ以外は「生産(gather)」補正（職種補正の対象が分岐）。

### 4.3 現場力（combat ステ）
| id | 表示 | 意味 |
|---|---|---|
| debug | デバッグ力 | 命中(手数) |
| impl | 実装力 | 火力(max hit) |
| robust | 堅牢性 | 被害軽減 |
| mental | メンタル | 精神HP。0で燃え尽き |

回復 = カフェイン / 栄養ドリンク / 有給（大回復）。

### 4.4 アイコン ★改訂
**絵文字は使わない。** `react-icons`（Simple Icons のブランドロゴ + Lucide の概念アイコン）を
`ui/icons.tsx` のレジストリ経由で使用。データは `icon: "react"` のような文字列idのみ持つ。

---

## 5. 敵 = 案件・上司（戦闘デザイン）

「モンスター」を **案件(プロジェクト)** と **上司(ボス)** に置換。雑魚は案件内の障害。

### 5.0 編成: フリーランス単発プール ＋ 会社ダンジョン（[0-8](#0-8-世界モデル-案件モンスター--会社ダンジョン) 参照）
- **単発案件プール**（無所属で常時受注可・低報酬）: バグ / 新人教育 / 仕様変更 等の汎用案件。プレイヤーLvでスケール。
- **会社ダンジョン**（所属でアクセス・高報酬・固有）: 会社ごとに**順序付きの案件列＋末尾に役員/上司ボス**。全クリアで**制覇**。
  - 例: 中小SIer →（バグ修正→仕様変更対応→納期前デスマ→**課長**）。大企業/外資ほど長く・強く・報酬大。
- 下記の各案件タイプ（5.1/5.2 のギミック）は、単発プールにも会社ダンジョンの中身にも配置できる共通パーツ。

### 5.1 雑魚 / 障害（旧monster）
| 敵 | ネタ | ギミック |
|---|---|---|
| バグ | 基本の雑魚 | 普通に殴る |
| 仕様変更 | 倒す直前にHPが回復することがある | 「仕様が変わりました」 |
| 新人教育 | 与ダメ低いが時間がかかる | 倒すと部下候補ドロップ |
| レビュー指摘 | こちらの設計力で軽減 | 防御依存 |
| 本番障害(インシデント) | 継続ダメージ(DoT=メンタル削り) | 早期撃破推奨 |

### 5.2 ボス = 上司
| ボス | 特徴 |
|---|---|
| 課長 | 最初のボス。残業要求でメンタル削り |
| 部長 | 「で、いつ終わるの？」прессで火力UP |
| 客先PM | 仕様変更を多重発生させる |
| 経営会議（ラスボス） | 全ギミック盛り。撃破でCTO昇進 |

### 5.3 メンタル & バーンアウト
- メンタル0 = 死亡ではなく **強制離脱（バーンアウト）**。資源ロスト無し、案件進捗だけリセット。
- 自動回復: メンタル50%以下で選択中のカフェイン/ドリンクを自動消費（既存の自動食事ロジック流用）。
- 「有給」= 高回復だが所持数が貴重（ショップ/評価報酬）。

> 既存 `Monster` 構造に `phases`（複数フェーズ）と `dot`（継続ダメ）を足すだけで大半が表現可能。

---

## 6. 装備 = ツール / 資格

旧weaponを **開発環境** に拡張。複数スロット。

| スロット | 例 | 効果 |
|---|---|---|
| エディタ(武器) | メモ帳 → VSCode → Vim → JetBrains | 実装力・速度 |
| マシン(防具) | 社用ノート → 自作PC → ワークステーション | メンタル上限・速度 |
| 資格(アクセ) | 基本情報 → AWS認定 → … | 各スキル補正 |
| 周辺機器 | HHKB / ウルトラワイド / ゲーミングチェア | 細かいバフ |

装備は **制作(DevOps/設計)** で作る or ショップ購入 or 案件ドロップ。

---

## 7. 部下システム ★Melvorの"暇"を解消

シニア昇進で **部下(ジュニア)** を採用可能。部下は自分とは別に作業を1つ持てる＝**並行運用**。

```ts
interface Subordinate {
  id: string; name: string;
  jobClass: string;      // 部下にも職種補正
  level: number;
  assignment: ActiveAction | null;  // 生産/制作/案件補助に割り当て
}
```

- 部下を **生産スキルに配置** → 放置中も素材が増える。
- 部下を **案件に同行** → 戦闘火力/メンタル肩代わり。
- 部下は「新人教育」案件や採用ショップで増える。育成・配置が **最適化パズル** になる。
- PM職は部下効率+25%（マネジメント特化ビルドが成立）。

> v1では未実装。エンジン的には「複数 activeAction の並行tick」への拡張が必要なので [§9](#9-将来拡張ロードマップ) 扱い。

---

## 8. 経済 / ショップ / 放置

- **通貨 = 円（給料）**。案件納品・評価で獲得。旧goldをリスキン。
- **ショップ**: カフェイン/ドリンク/有給、装備、資格教材、部下採用。
- **オフライン進行**: 「放置＝勤務時間が自動で進む」。既存の24hキャップ・welcome backをそのまま使用（文言だけ「おかえりなさい、〇時間ぶんの業務が進みました」に）。

---

## 9. 将来拡張ロードマップ

| フェーズ | 内容 |
|---|---|
| **v1（今のエンジンのリスキン）** | 生産4 / 制作3スキル、戦闘ステ4種、案件(雑魚)5体＋ボス「課長」、装備=エディタ1スロット、メンタル&カフェイン、ショップ、オフライン。**既存dataの差し替えだけで到達可能。** |
| v2 | 職種クラスチェンジ＋補正、資格アクセ、ボス追加 |
| v3 | 部下システム（並行tick）、案件の複数フェーズ/DoT |
| v4 | 昇進ツリー、CTOプレステージ（転生）、ランダム依頼(案件ボード) |

---

## 10. エンジンへのマッピング（実装メモ）

| 仕様 | 既存構造 | 変更点 |
|---|---|---|
| 生産/制作スキル | `data/skills.ts`, `data/actions.ts` | 中身を差し替えるだけ |
| 戦闘ステ | `data/skills.ts`(combat) + `combat.ts` | id/表示名リスキン |
| 案件/上司 | `data/monsters.ts` | 後で `phases`/`dot` 追加 |
| 装備 | `data/items.ts`(weapon) | 後でマルチスロット化 |
| 円/ショップ/オフライン | `store.ts`, `progression.ts` | 文言リスキンのみ |
| クラス補正 | 新規 `data/classes.ts` + modifier適用 | v2 |
| 部下 | 新規 + 並行tick | v3 |

---

---

## 11. 実装ステータス（生きた台帳）

| ver | 内容 | 状態 |
|---|---|---|
| v1 | IT テーマ総入れ替え（生産4/制作4スキル、案件6＝バグ〜課長、エディタ3、メンタル&カフェイン、円、UI日本語化）。combatスタット参照を `STAT` に集約。saveバージョンガード(v2)。 | ✅ 完了 |
| v2 | 職種クラスチェンジ＋補正。`modifiers.ts`(効果合算) + `effects.ts`(集約) + `rank.ts`(出世) + `data/classes.ts`(8職種)。生産/制作速度・XP・戦闘ステ・給料・ドロップに補正。キャリア画面で役職進捗＆職種選択。save v3。 | ✅ 完了 |
| v3 | 部下システム（`team.ts`: 並行作業tick・採用費・部下効率）。役職で採用枠解禁。部下に生産/制作を割り当て→放置でも並行生産・部下も育つ。PM職に部下効率+30%。案件ギミック: DoT(本番障害=メンタル継続削り)・regen(仕様変更=自己回復)。チーム画面。offline並行シミュ。save v4。 | ✅ 完了 |
| v4 | 起業プレステージ(転生)＋経営ツリー(`data/prestige.ts`)。シニア以上で起業→ストック獲得→永続強化7種(資金調達/自動化/技術力/ブランド/メンタルケア/採用力/開発力)。`effects.ts`に合流。save v5。 | ✅ 完了 |
| **改訂1** | **tech-stack化**: 粗い生産/制作スキルを廃し、言語→フレームワーク(依存`requires`付き)へ。**絵文字を全廃し react-icons へ**(`ui/icons.tsx`)。エディタはショップ購入に。save v6。 | ✅ 完了 |
| **改訂2** | **大量化＋Melvor風グルーピング**: 言語30(系統別7グループ)。サイドバーを**折りたたみ式**(`data/groups.ts`)に。各技術に実ブランドロゴ。 | ✅ 完了 |
| **改訂3** | **フレームワークを言語の傘下へ**: 独立スキルを廃し、各言語スキル配下に base/概念/ライブラリ/フレームワーク/OSS/資格 のアクション(パターン)を生成(`data/techtree.ts`)。SkillView はカテゴリ別表示＋**解禁前も表示して開始不可**。craft補正は framework アクションのみ対象に。 | ✅ 完了 |
| **改訂4** | **装備＋生活クラフト**: 装備を5スロット化(`equipment`: デバイス武器/服/髪/アイコン/PC)。非武器は modifier 補正、武器は戦闘ステ。**料理**スキル(食材→飲食物で回復)＋**PC組み立て**スキル(CPU/GPU/RAM/SSD→PC装備)。ショップを刷新(飲食物/食材/PCパーツ/デバイス/服髪アイコンを `buyItem`)。装備タブ新設。save v7。 | ✅ 完了 |
| v5+ | 案件ボード/ランダムイベント/資格/オフィス施設/技術スタック相性(案件×分野)… | 未 |

> 実装は `止まるな` 方針で v1 から順に積み上げ中。各 ver 完了時に typecheck + build + offline test を通してコミット。

---

### 用語リスキン早見表
gather→生産 / craft→制作 / combat→遂行 / monster→案件 / boss→上司 / weapon→エディタ / food→カフェイン / HP→メンタル / gold→円 / level→熟練度 / combat level→エンジニアレベル
